# Nordic Museum Årets dagar
React Native application for iOS and Android.

### Installation
- Clone repository and then:
```npm install```

- Create a file called .env in project root and add.
```
GOOGLE_MAPS_API_KEY=your_key
NM_API_URL=
```
